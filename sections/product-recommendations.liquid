{{ 'swiper-bundle.min.css' | asset_url | stylesheet_tag }}
{{ 'product-recommendations.css' | asset_url | stylesheet_tag }}
<script src="{{ 'swiper-bundle.min.js' | asset_url }}" defer></script>
<script src="{{ 'product-recommendations.js' | asset_url }}" defer></script>

<section class="collection" data-product-id="{{ product.id }}">
  <div class="wrapper">
    <h2 class="collection__header">You may also like</h2>

    <div class="swiper collection__swiper">
      <div class="swiper-wrapper collection__cards"></div>
      <div class="swiper-button-prev" aria-label="Previous slide"></div>
      <div class="swiper-button-next" aria-label="Next slide"></div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Product Recommendations",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Section heading",
      "default": "Recommended products"
    }
  ],
  "presets": [
    {
      "name": "Product Recommendations Carousel",
      "category": "Products"
    }
  ]
}
{% endschema %}
